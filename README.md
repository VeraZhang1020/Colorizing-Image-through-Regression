# Colorizing-Image-through-Regression
This project builds models to colorize grayscale images using linear regression (gradient descent), SVR, a neural network baseline, and U-Net. Due to the large size of ImageNet, we use the Tiny ImageNet dataset from Kaggle as a manageable subset for training and evaluation.
